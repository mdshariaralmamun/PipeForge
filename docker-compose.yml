services:
  web:
    build:
      context: .
      args:
        # Version stamp baked into the build (deploy.yml exports these;
        # empty for local `docker compose up` — next.config falls back to git)
        APP_VERSION: ${APP_VERSION:-}
        APP_BUILD_TIME: ${APP_BUILD_TIME:-}
    restart: unless-stopped
    expose:
      - '3000'

  # Reverse proxy with automatic HTTPS.
  # Only start this when PipeForge is the ONLY site on the server:
  #   docker compose --profile proxy up -d --build
  # If price.shariar.dev already runs its own Caddy on this VPS, do NOT start
  # a second one — add the pipeforge.shariar.dev block to the existing
  # Caddyfile instead (see DEPLOY.md).
  caddy:
    image: caddy:2-alpine
    profiles: ['proxy']
    restart: unless-stopped
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - web

volumes:
  caddy_data:
  caddy_config:
